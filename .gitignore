bin/
obj/
*.user
*.suo
.vs/
.idea/

# SQLite vector stores + recency pointer (created by ingest, never shipped)
*.db
*.db-journal
*.db-shm
*.db-wal
*.cs-agent-recency

# OS noise
.DS_Store

# Local env (BYO keys are env vars; keep them out of the repo)
.env
.env.*

# eval output (regenerated by `cs-agent eval`)
eval-results/
.cs-agent-recency
