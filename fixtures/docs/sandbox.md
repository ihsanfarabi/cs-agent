# Sandbox

## One sandbox per account

Every account gets a sandbox project at signup. The sandbox is isolated from production data.

## Resetting

Reset the sandbox with `nimbus sandbox reset`. Resetting deletes all sandbox data and restores the default template.

