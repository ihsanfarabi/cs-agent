# Authentication

## Personal access tokens

Personal access tokens authenticate API requests on behalf of a user. Create a token from Settings → Access tokens. Tokens carry the same scopes the user has.

## OAuth apps

Third-party applications can request access through OAuth. OAuth apps request scopes explicitly, and users approve each scope during authorization.

