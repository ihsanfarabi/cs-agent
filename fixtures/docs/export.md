# Export

## Export formats

Export project data as JSON or CSV from Settings → Export. JSON exports preserve structure; CSV exports flatten to one row per record.

## Snapshots

Point-in-time snapshots are available through the API for Enterprise workspaces.

