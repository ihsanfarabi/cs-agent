# Migrations

## Importing projects

Import an existing project with `nimbus import`. The command connects to the source repository and copies configuration.

## Schema migrations

Schema migrations run automatically during each deploy. Migrations run in order and stop the deploy if one fails.

