# Audit logs

## What is recorded

Audit logs record every configuration change: role changes, key rotation, webhook edits, and setting updates. Each entry records the actor, the action, and the timestamp.

## Retention

Audit logs are kept for 90 days on all plans. Enterprise workspaces can extend retention to one year.

## Exporting

Export audit logs through the audit API endpoint as JSON.

