# Quickstart

## Create a project

Create your first project from the dashboard. A project groups deployments, settings, and collaborators.

## Deploy

Run `nimbus init` in your repository, then `nimbus deploy`. The first deploy runs a free build.

